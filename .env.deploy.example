# scripts/build-and-ship.sh reads this file.
# Copy to .env.deploy (gitignored) and fill in.
#
# SSH key auth must already work:
#   ssh-copy-id $DEPLOY_USER@$DEPLOY_HOST
#   ssh $DEPLOY_USER@$DEPLOY_HOST "echo ok"   # should not prompt for password

DEPLOY_HOST=5.78.181.55
DEPLOY_USER=hhmack
DEPLOY_PATH=/opt/go-participate
