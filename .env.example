# =================================
# Illuminate V3 SaaS Platform
# =================================

# MongoDB — Platform database (tenants, users, plans, feature flags)
# Tenant databases are auto-created as "tenant_<slug>" on the same cluster.
# e.g. tenant "acme-meat-co" → database "tenant_acme_meat_co"
#
# Local (docker compose up):
MONGODB_URI=mongodb://illuminate:illuminate_dev@localhost:27017/illuminate_platform?authSource=admin
# Production (Atlas):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/illuminate_platform?retryWrites=true&w=majority

# NextAuth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-here-generate-with-openssl-rand-base64-32

# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Stripe — only need your account keys, NOT individual price IDs.
# Price IDs are stored in the Plan documents in MongoDB (set via seed or admin UI).
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Email (Resend)
RESEND_API_KEY=re_...
EMAIL_FROM=Illuminate <noreply@yourdomain.com>

# AI (Anthropic Claude)
ANTHROPIC_API_KEY=sk-ant-...

# Redis
# Local (docker compose up):
REDIS_URL=redis://localhost:6379
# Production (Upstash):
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=

# Object Storage (S3-compatible)
S3_BUCKET=illuminate-uploads
S3_REGION=us-east-1
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_ENDPOINT=

# Platform Domain — used for subdomain-based tenant routing
# Tenants are accessed at: <slug>.meatlocker.app
# Comma-separated for multiple domains (e.g. staging + production)
NEXT_PUBLIC_PLATFORM_DOMAIN=meatlocker.app

# App URLs
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_ADMIN_URL=http://localhost:3001
NEXT_PUBLIC_DASHBOARD_URL=http://localhost:3002
NEXT_PUBLIC_STOREFRONT_URL=http://localhost:3003
